# X Post (FINAL — superseded storyboard lives in demo-video.md)

## The post (copy-paste exactly; 276/280 weighted chars, includes required #OKXAI and the direct agent link)

```
AI agents lie about data. Plotline makes that structurally impossible.

CSV in → cinematic data story out. Every fact computed, every claim signed, and ANY agent can re-verify everything — six attacks, zero survived.

Live on #OKXAI — talk to it:
🔗 https://www.okx.ai/agents/5106
```

Attach `plotline-demo.mp4`. Media doesn't count against the 280 limit.
Do NOT add bare "OKX.AI" text — X auto-links it and burns 23 characters.

Reply 2 (post immediately after, links go here so they don't crowd the main post):

```
See one for yourself — a 14-month startup CSV turned into this, in one tool call:
https://plotline-production-34e6.up.railway.app/story/b1h4v9ta68

Or search "Plotline" on OKX.AI. Free to call, MCP-native, x402-ready.
```

## 90-second demo storyboard (OLD — use submission/demo-video.md instead)

| t | Shot | Voiceover / caption |
|---|---|---|
| 0–8s | Black screen, typing: `"agents misquote data"` → cut to Plotline landing hero | "LLMs hallucinate statistics. That's an architecture problem — not a prompt problem." |
| 8–20s | Landing playground: click **SaaS metrics** sample, click **Generate story** | "This is Plotline. Paste any CSV — or let your agent POST it." |
| 20–50s | The story page: hero count-up (+162.9%), slow scroll through scenes — line chart draws itself, changepoint scene, outlier pulse, scatter with r badge | "A deterministic stats engine computes the facts. The AI only narrates them. Every number is audited against the fact ledger." |
| 50–62s | Scroll to fact-ledger appendix, hover a row; zoom on footer audit line | "Every story ships with its proof. Claim, value, method." |
| 62–78s | Terminal/agent chat: MCP `create_story` tool call returning a URL; open the neon-theme crypto story | "It's MCP-native — one tool call from any agent. Listed on OKX.AI, payable with x402 on X Layer." |
| 78–90s | Landing page pricing → Plotline logo + URL + #OKXAI | "Plotline. Numbers from code. Words from AI. Never the reverse. Live on OKX.AI." |

Recording tips:
- 1080p, dark room aesthetic; the midnight theme is the star — scroll SLOWLY.
- Use the saas-metrics sample for the main story (best changepoint) and the
  okb-daily sample in neon for the crypto beat.
- Keep the MCP call visible for ~5s so judges see the protocol actually work.
